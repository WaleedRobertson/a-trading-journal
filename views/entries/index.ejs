<!DOCTYPE html>
<html lang="en">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Journal</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Welcome to my Journal</h1>
    <a href="/users/<%= user._id %>/entries/new">Add New Item</a>
    <% if (journal.length > 0) { %>
        <% journal.forEach((food) => { %>
            <a href="/users/<%= user._id %>/entries/<%= food._id %>">
                <li><%= food.name %></li>
            </a>
        <% }); %>
    <% } else { %>
        <p>You haven't tracked any trades with us.</p>
    <% } %>
  </body>
</html>